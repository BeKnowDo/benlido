<ul class="social-media-links">
    {% for item in socialMediaLinks %}
        <li>
            <a href="{{item.url}}" title="{{item.alt_title}}">{{item.title}}</a>
        </li>
    {% endfor %}
</ul>